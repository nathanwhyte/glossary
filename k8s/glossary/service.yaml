---
apiVersion: v1
kind: Service
metadata:
  name: glossary
  namespace: glossary
  labels:
    app: web
spec:
  type: ClusterIP
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
  selector:
    app: web

