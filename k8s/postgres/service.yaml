---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: glossary
  labels:
    app: db
spec:
  type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30000
      protocol: TCP
      name: postgres
  selector:
    app: db

